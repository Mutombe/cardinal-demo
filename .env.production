# Public marketing website — production build config (read by `npm run build`).
# These are public Vite vars (no secrets).

# ERP admin app — the footer "Staff login" button links to ${VITE_ERP_URL}/login
VITE_ERP_URL=https://cardinal-erp.onrender.com

# ERP API — website inquiries + live stand availability
VITE_ERP_API=https://cardinal-backend-tm3x.onrender.com
